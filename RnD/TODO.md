# AM5 development notes

- remove data from item.h and use oneData instead
- implement withId()
- add function `printItem` (AM5 specific, with context) and use  
  `void print(...)` for regular data output